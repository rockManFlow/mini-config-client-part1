spring:
  cloud:
    config:
      name: mini-config-client-part1 # 需要从github上读取资源名称 注意没有yml 这个是配置文件名
      profile: test # 本次访问的配置项-可以设置环境变量
      label:  master #分支版本
      uri: http://localhost:3366 # 配置中心地址